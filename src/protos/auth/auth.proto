syntax = "proto3";

option go_package = "auth/";

package auth;

message User {
    string userName = 1;
    string passwd = 2;
    bool isActive = 3;
}

message LoginResponse {
    string token = 1;
}

service Auth {
    rpc Login (User) returns (LoginResponse) {}
}